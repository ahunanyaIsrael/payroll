import SideBar from '../../components/SideBar/SideBar'

export const Employer = () => {
  return (
    <div className="employer-page">
      <SideBar /> {/* always visible on employer pages */}
    </div>
  )
}
